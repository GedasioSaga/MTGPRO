[package]
name = "mtg-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
rand.workspace = true
rand_chacha.workspace = true
smallvec.workspace = true

# Só para o teste de fumaça: a partida precisa de cartas e decks reais para
# exercitar resolve/triggers/combat. Ciclo de dev-dependency é permitido pelo
# cargo e não entra no grafo de build da lib.
[dev-dependencies]
mtg-cards = { path = "../mtg-cards" }
