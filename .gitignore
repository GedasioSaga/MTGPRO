target/
node_modules/
web/dist/
*.db
*.sqlite
.claude-trail.md
graphify-out/
docs/bar/
target-probe/
target*/
docs/shots/
.playwright-mcp/
data/
.cache/
*.sqlite-wal
*.sqlite-shm

# Bulk do Scryfall: 23 MB de dado de terceiro que muda todo dia.
.cache/scryfall/
# Catalogo do jogo: `data/catalog.db` (padrao) ja cai em `data/` e em `*.db`.
# `catalog.sqlite` era o banco proprio do importador, hoje sem uso — a entrada
# fica so para nao commitar copia velha que ainda exista na maquina de alguem.
catalog.sqlite
